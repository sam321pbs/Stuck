<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:orientation="vertical"
             android:background="@color/colorPrimary"
             android:layout_width="match_parent"
             android:layout_height="match_parent">

    <TextView
            style="@style/stuck_title_main_page"/>

    <LinearLayout android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:orientation="vertical"
                  android:layout_gravity="center_horizontal|center_vertical"
                  android:layout_marginBottom="40dp"
                  android:layout_marginTop="20dp">

        <TextView android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:layout_marginBottom="40dp"
                  android:text="Type your email address below so we can send you a temporary password"
                  android:textColor="@color/colorWhite"
                  android:layout_marginStart="@dimen/activity_horizontal_margin"
                  android:layout_marginEnd="@dimen/activity_horizontal_margin"
                  android:gravity="center"
                  android:textSize="16sp"/>

        <EditText
                android:id="@+id/forgot_password_email_edit_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/login_edit_text_box"
                android:hint="@string/type_your_email_here"
                android:inputType="textEmailAddress"/>

        <TextView
                android:layout_marginTop="40dp"
                android:id="@+id/forgot_password_sign_up_accout"
                android:text="@string/need_an_account_sign_up"
                style="@style/login_style_text"/>


    </LinearLayout>

    <Button
            android:id="@+id/send_temp_button"
            android:background="@drawable/login_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="@dimen/login_sign_up_button_from_bottom"
            android:text="Send Temp Password"/>


</FrameLayout>